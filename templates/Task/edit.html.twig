{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title.task_edit'|trans({'%id%': task.id|default('')}) }}
{% endblock %}

{% block body %}
    <h1>{{ 'title.task_edit'|trans({'%id%': task.id|default('')}) }}</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="centered-container">
        <input type="submit" value="{{ 'action.save'|trans }}" class="btn btn-primary"/>
    </div>
    <div class="centered-container">
        <a class="create" href="{{ url('task_index') }}" title="{{ 'action.back_to_list'|trans }}">
            {{ 'Powrót'|trans }}
        </a>
    </div>
    {{ form_end(form) }}
{% endblock %}