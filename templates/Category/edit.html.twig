{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title.category_edit'|trans({'%id%': category.id|default('')}) }}
{% endblock %}

{% block body %}
    <h1>{{ 'title.category_edit'|trans({'%id%': category.id|default('')}) }}</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="centered-container">
        <input class="create" type="submit" value="{{ 'action.save'|trans }}" class="btn btn-primary"/>
    </div>
    <div class="centered-container">
        <a class="create" href="{{ url('category_index') }}" title="{{ 'action.back_to_list'|trans }}">
            {{ 'Powrót'|trans }}
        </a>
    </div>
    {{ form_end(form) }}
{% endblock %}